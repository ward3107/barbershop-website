# Firebase Configuration (SAFE - designed to be public)
# Get these values from: Firebase Console > Project Settings > General > Your apps > Web app
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Owner Contact Information (SAFE - public contact info)
VITE_OWNER_WHATSAPP=972XXXXXXXXX
VITE_OWNER_PHONE=972XXXXXXXXX
VITE_OWNER_EMAIL=owner@shokhabarbershop.com

# Make.com Webhook (SAFE - just a webhook URL)
VITE_MAKE_WEBHOOK_URL=your_make_webhook_url

# EmailJS Configuration (SAFE - designed for frontend use)
VITE_EMAILJS_SERVICE_ID=your_emailjs_service_id
VITE_EMAILJS_TEMPLATE_ID=your_emailjs_template_id
VITE_EMAILJS_USER_ID=your_emailjs_user_id

# HCaptcha (SAFE - site key is designed to be public)
VITE_HCAPTCHA_SITE_KEY=your_hcaptcha_site_key

# ========================================
# ⚠️ TEMPORARY PASSWORDS (NOT SECURE):
# ========================================
# WARNING: These passwords are still visible in the frontend bundle.
# This is a TEMPORARY solution. For production, you MUST implement:
# 1. Firebase Authentication with custom claims for roles (admin/owner)
# 2. Backend API to verify roles
# 3. Never store passwords in frontend code or environment variables
VITE_ADMIN_PASSWORD=change_this_admin_password
VITE_OWNER_PASSWORD=change_this_owner_password

# ========================================
# ⚠️ REMOVED FOR SECURITY:
# ========================================
# The following have been REMOVED because they should NEVER be in frontend code:
# - VITE_TWILIO_ACCOUNT_SID (DANGEROUS - move to backend)
# - VITE_TWILIO_AUTH_TOKEN (DANGEROUS - move to backend)
# - VITE_TWILIO_WHATSAPP_NUMBER (DANGEROUS - move to backend)
# - VITE_TELEGRAM_BOT_TOKEN (DANGEROUS - move to backend)
# - VITE_TELEGRAM_CHAT_ID (DANGEROUS - move to backend)
# - WEBHOOK_SECRET (DANGEROUS - backend only)
#
# If you need SMS/WhatsApp notifications, implement a backend API endpoint
# and call it from your frontend instead of using credentials directly.

